
tasks:
  - init: | 
      python -m venv venv
      source venv/bin/activate
      pip install -r requirements.txt
    command: |
      source venv/bin/activate
      ./openai-key-init.sh
vscode:
  extensions:
    - ms-python.python
    - ms-toolsai.jupyter
